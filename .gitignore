# =====================================================================
# VENOMICA QUANTUM MATRIX: AUTONOMOUS AGI ARTIFACTS
# =====================================================================

# 1. The Phantom Driver & Execution Binaries
mutation_driver.cpp
mutation_core_bin
*mutation_core_bin*

# 2. Forensic Quarantine Vault
# (NEVER track quarantined malware or failed mutations to prevent GitHub bans)
/quarantine/*
!/quarantine/.gitkeep

# 3. Telemetry & Neural Ledgers
# (Ignore runtime lock files and volatile sync data, but keep the folders)
/meta/*.lock
/meta/sync_network/*
!/meta/sync_network/.gitkeep
/meta/status.json

# 4. State Machine Locks
*.lock
!KILL_SWITCH.lock
